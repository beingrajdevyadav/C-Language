#include <stdio.h>
// int data type is used to store non - decimals values
int main(){
    int x = 5;
    int y = 2;

    printf("%d", x + y);
    printf("\n%d", x - y);
    printf("\n%d", x * y);
    printf("\n%d", x / y);

    return 0;
}